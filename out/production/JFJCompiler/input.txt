PROGRAM programa_prueba;

FUNC UINT f(DOUBLE parametro)
UINT y,x;
BEGIN
    y := -3 * x;
    RETURN (y);
    POST: (y < 3), %Error en la postcondicion%;
END;

UINT B, A;
BEGIN
    f(B);
    IF (A > B) THEN
    BEGIN
        B := A;
        A := B;
    END;
    ELSE
    BEGIN
        A := 10;
        B := 20;
    END;
    ENDIF;

    B := 1;
END;